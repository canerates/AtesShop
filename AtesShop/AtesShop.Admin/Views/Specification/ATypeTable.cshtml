@model List<AttributeTypeViewModel>

@{
    ViewBag.Title = "ATypeTable";
}

<div class="container">
    <div class="page-header">
        <h2>Attribute Types</h2>
    </div>
</div>

<div class="container" id="tableContainer">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>AttributeType Key</th>
                <th>Culture</th>
                <th>AttributeType Value</th>
            </tr>
        </thead>
        <tbody>
            <tr><td colspan="5"></td></tr>
            @foreach (var elem in Model)
            {
                for (int i = 0; i < elem.ResourceCount; i++)
                {
                    <tr>
                        @if (i == 0)
                        {
                            <td rowspan="@elem.ResourceCount">@elem.ATypeResources[i].Key</td>
                        }


                        <td>@elem.ATypeResources[i].Culture</td>
                        <td>@elem.ATypeResources[i].Value</td>

                    </tr>
                }
                <tr><td colspan="5"></td></tr>
            }
        </tbody>
    </table>
</div>


